<UserControl x:Class="ShowMaker.Desktop.Modules.Storyboard.Views.StoryboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Tree.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.11*"/>
            <RowDefinition Height="0.89*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.405*" />
            <ColumnDefinition Width="0.004*" />
            <ColumnDefinition Width="0.591*" />
        </Grid.ColumnDefinitions>
        <!-- 左侧 -->
        <!-- 展区、设备、操作工具面板 -->
        <StackPanel x:Name="itemToolsPanel" Margin="0,0,0.2,0" Orientation="Horizontal">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="#FF707070" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <!-- 添加(展区、设备) -->
            <Button Content="+" HorizontalAlignment="Left" Height="20" Width="20" ToolTip="添加"/>
            <!-- 删除 -->
            <Button Content="-" HorizontalAlignment="Left" Height="20" Width="20" ToolTip="删除"/>
        </StackPanel>
        <!-- 展区、设备、操作树 -->
        <TreeView x:Name="showAreaTree" Grid.ColumnSpan="2" Margin="0,0,0.2,0" Grid.Row="1" AllowDrop="True">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Drop">
                    <cal:ActionMessage MethodName="OnDeviceItemDrop">
                        <cal:Parameter Value="$source"/>
                        <cal:Parameter Value="$eventArgs"/>
                    </cal:ActionMessage>
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <TreeViewItem ItemsSource="{Binding SelectedExhibition.AreaItems}">
                <TreeViewItem.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="展示会">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <cal:ActionMessage MethodName="OnExhibitionClick">
                                    <cal:Parameter Value="$source"/>
                                    <cal:Parameter Value="$eventArgs"/>
                                    </cal:ActionMessage>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>
                    </DataTemplate>
                </TreeViewItem.HeaderTemplate>
                <TreeViewItem.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding DeviceItems}">
                        <!-- 展区名 -->
                        <TextBlock Text="{Binding Path=Name}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <cal:ActionMessage MethodName="OnAreaItemClick">
                                        <cal:Parameter Value="$source"/>
                                        <cal:Parameter Value="$eventArgs"/>
                                        <cal:Parameter Value="{Binding Path=.}"/>
                                    </cal:ActionMessage>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>
                        <HierarchicalDataTemplate.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding OperationItems}">
                                <!-- 设备 -->
                                <TextBlock Text="{Binding Path=Type}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseLeftButtonDown">
                                            <cal:ActionMessage MethodName="OnDeviceItemClick">
                                                <cal:Parameter Value="$source"/>
                                                <cal:Parameter Value="$eventArgs"/>
                                                <cal:Parameter Value="{Binding Path=.}"/>
                                            </cal:ActionMessage>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBlock>
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <DataTemplate>
                                        <!-- 操作 -->
                                        <TextBlock Text="{Binding Path=Name}">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                                    <cal:ActionMessage MethodName="OnOperationItemClick">
                                                        <cal:Parameter Value="$source"/>
                                                        <cal:Parameter Value="$eventArgs"/>
                                                        <cal:Parameter Value="{Binding Path=.}"/>
                                                    </cal:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </TextBlock>
                                    </DataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeViewItem.ItemTemplate>
            </TreeViewItem>
        </TreeView>

        <GridSplitter Grid.Column="1" Name="gridSplitter1" Grid.RowSpan="2" Width="3" Margin="0,0,-0.4,8" HorizontalAlignment="Center" />

        <!-- 右侧 -->
        <!-- 播放工具面板 -->
        <StackPanel x:Name="playToolsPanel" Grid.Column="2" Margin="0.6,0,0,0" Orientation="Horizontal">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="#FF707070" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <!-- 播放 -->
            <Button Content="B" HorizontalAlignment="Left" Height="20" Width="20" ToolTip="播放"/>
            <!-- 暂停 -->
            <Button Content="P" HorizontalAlignment="Left" Height="20" Width="20" ToolTip="暂停"/>
            <!-- 停止 -->
            <Button Content="S" HorizontalAlignment="Left" Height="20" Width="20" ToolTip="停止"/>
        </StackPanel>
        <!-- 时间线 -->
        <Canvas x:Name="timelineCanvas" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2">
            <Path Stroke="Black" Data="M 0,20 H 150,20 V 80" />
        </Canvas>
    </Grid>
</UserControl>
